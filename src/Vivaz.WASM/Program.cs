using System;

namespace Vivaz.WASM
{
    public static class Program
    {
        // Minimal entry point to satisfy publish for browser-wasm; does nothing at runtime.
        public static int Main(string[] args)
        {
            return 0;
        }
    }
}
