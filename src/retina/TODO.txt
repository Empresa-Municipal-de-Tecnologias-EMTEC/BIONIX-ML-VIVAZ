TODO: Complete RetinaFace migration to conv→FPN→conv-heads and productionize

High-level goals:
- Replace all legacy BlocoCNN usage with tensor-backed RetinaFace fields and RetinaFace methods
- Implement production-quality backbone (depthwise separable convs) and FPN
- Implement spatial conv heads per FPN level (3x3 or separable stacks) producing full output maps
- Vectorize trainer to use FPN maps and batched head outputs
- Update persistence and WASM bundle format to include new tensors
- Add smoke tests and CI checks
- Optimize and iterate: separable convs, quantization, perf

Detailed task list (order recommended):
1) Replace remaining BlocoCNN call sites (~20 occurrences)
   - Files: src/wasm/build_bundle.mojo, src/wasm/arcface_wasm.c (bundle reader), BIONIX-ML-EXEMPLOS, model_detector.mojo, others
   - Approach: use RetinaFace/ArcFace tensor-backed fields (`bloco_kernels`, `bloco_peso_saida`, `bloco_bias_saida`) or add small adapter methods on the structs
2) Ensure `RetinaFace` fully encapsulates model tensors
   - Confirm `extrair_features_patch` exists and covers training/inference needs
3) Implement spatial conv heads
   - Update `retina_modelo.mojo` to store per-level conv weights and biases and to predict maps instead of pointwise dots
4) Implement separable conv backbone
   - Use dispatcher conv and avgpool helpers; persist weights
5) Vectorize trainer (`retina_trainer.mojo`)
   - Replace per-anchor per-patch loops with batched operations over head output maps
   - Map anchors to grid indices per-level efficiently
6) Update persistence
   - `salvar_workspace` / `carregar_workspace` must include backbone/FPN/head tensors, shapes, and `tipo_computacao`
7) Update WASM bundle and arcface code
   - `wasm/build_bundle.mojo` should write the new tensor blobs
   - `wasm/arcface_wasm.c` must be updated to read new blob formats (or keep compatibility)
8) Smoke test: save/load roundtrip + 1-batch train + inference compare
9) Performance and determinism
   - Optimize convs (separable), consider deterministic kernels, save optimizer state if needed

Notes:
- Training re-entrancy (bit-identical) requires saving optimizer/RNG state; currently not implemented.
- Prioritize backward compatibility by keeping legacy blobs during transition.

10) WASM bridge / browser integration (new)
   - Expose full VIVAZ features to browser via WASM + JS bridge: load bundle, run detection/inference, get callbacks/events for detections, send identification events, support model reloading.
   - Update `wasm/build_bundle.mojo` and `wasm/arcface_wasm.c` to produce/read the new tensor-backed bundle format.
   - Provide a small JS glue layer and example HTML to demonstrate detection→identification→event dispatch.
   - Add build task to `pixi` scripts to compile with emscripten and produce `arcface_wasm.wasm` and glue code.

Status keys:
 - [ ] Pendente
 - [X] Completa
 - [-] Em andamento

Current status:
 - [ - ] Replace remaining BlocoCNN call sites
 - [ X ] Implement conv-based backbone (MobileNet-like)
 - [ X ] Implement conv FPN fusion and per-level conv heads
 - [ X ] Adapt anchor generator to per-level usage
 - [ ] Vectorize trainer to use conv-FPN outputs (replace patch loops)
 - [ X ] Update persistence for new head/backbone tensors
 - [ ] Run smoke test: 1-batch train + inference (save/load roundtrip)
 - [ - ] Refactor wasm bundle & other callers referencing BlocoCNN
 - [ ] Iterate on true separable convs and pointwise optimization
 - [ ] WASM bridge / browser integration

Next action: patch `src/wasm/build_bundle.mojo` to read kernels and head tensors from tensor-backed structs (`bloco_kernels`, `bloco_peso_saida`, `bloco_bias_saida`, `proj_peso`) instead of `bloco_cnn`.
